# Frontend

Single-page React UI for initiating PSTN calls, showing status timeline, and streaming transcripts via WebSocket.

## Setup

```bash
npm install
npm run dev
```

The dev server proxies API calls to `http://localhost:3000`.
